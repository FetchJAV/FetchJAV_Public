# Headless downloader deps only (no Pillow / customtkinter / tkinter — the container
# runs docker_cli.py, not the GUI). ffmpeg comes from the base image via apt.
requests>=2.28.0
beautifulsoup4>=4.11.0
m3u8>=3.0.0
pycryptodome>=3.15.0
cloudscraper>=1.2.68
curl_cffi>=0.7.0
certifi
